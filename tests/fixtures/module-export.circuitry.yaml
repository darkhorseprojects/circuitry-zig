circuitry: "0.5"
imports:
  shared: ./module-shared.circuitry.yaml
exports:
  main:
    run: shared.assistant
    input:
      question: string
      count: integer
      debug: boolean
      tags:
        list: string
      meta:
        level: integer
resources: {}
